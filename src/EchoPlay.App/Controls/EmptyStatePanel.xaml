<?xml version="1.0" encoding="utf-8"?>
<!-- Wiederverwendbarer Leer-Zustand mit Icon, Text und optionalem Aktions-Button.
     Wird in Mediathek-Seiten für "Keine Serien", "Kein Provider" etc. verwendet. -->
<UserControl
    x:Class="EchoPlay.App.Controls.EmptyStatePanel"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel
        HorizontalAlignment="Center"
        VerticalAlignment="Center"
        Spacing="12"
        Padding="32">

        <FontIcon
            x:Name="IconElement"
            FontSize="48"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            HorizontalAlignment="Center"/>

        <TextBlock
            x:Name="MessageText"
            Style="{ThemeResource BodyTextBlockStyle}"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            HorizontalAlignment="Center"
            TextAlignment="Center"
            TextWrapping="Wrap"
            MaxWidth="400"/>

        <Button
            x:Name="ActionButton"
            HorizontalAlignment="Center"
            Visibility="Collapsed"/>
    </StackPanel>
</UserControl>
